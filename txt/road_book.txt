- NO Windows, todas as instalações do python devem ser feita no Miniforge Prompt
- instalar miniforge (https://github.com/conda-forge/miniforge)
    - No Windows adicionar C:\ProgramData\miniforge3 à PATH
- instalar portaudio (apenas MacOS)
    Para mac ARM
    $ arch -arm64 brew install portaudio
- Instalar o python e dependencias:
    - pyaudio
        Para mac ARM
        $ arch -arm64 pip install --no-cache-dir --global-option='build_ext' --global-option='-I/opt/homebrew/include' --global-option='-L/opt/homebrew/lib' 
        Windows
        $ python -m pip install pyaudio
    - numpy (conda)
    - scipy (conda - para ler ficheiros wave nos testes)
    - matplotlib (conda - para plotar os sinais durante os testes)
    - $ pip install ffmpeg-python
    - $ pip install pyserial

- Teste e criação de objeto para processar o código de linha LTC. OK
- Teste do objeto na leitura de dados em chunks. OK
- Teste do objeto lendo um canal de áudio em tempo real (modo bloqueante com limite de tempo). OK
- Teste em modo não bloqueante OK
- Criação de plugin para OBS OK
- Criação de GPIO to serial interface